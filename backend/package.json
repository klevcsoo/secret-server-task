{
  "name": "backend",
  "version": "1.0.0",
  "main": "build/main.js",
  "author": "Keve Lajsz",
  "scripts": {
    "prestart": "tsc",
    "start": "node .",
    "build": "tsc && mkdir -p build/public && rsync --progress ../client/build/* build/public/",
    "predeploy": "npm run build",
    "deploy": "cd .. && git subtree push --prefix backend heroku master"
  },
  "devDependencies": {
    "@types/node": "^18.0.3",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@types/cors": "^2.8.12",
    "@types/cryptr": "^4.0.1",
    "@types/express": "^4.17.13",
    "cors": "^2.8.5",
    "cryptr": "^6.0.3",
    "express": "^4.18.1",
    "mongodb": "^4.7.0"
  }
}
